# Copy this file to asset_forge/.env and fill in the key for the backend you use.
# .env is gitignored (never commit a key). The default 'pollinations' backend needs NEITHER.
GEMINI_API_KEY=
# For --backend hf (free Hugging Face Inference API): a token from huggingface.co/settings/tokens
HF_TOKEN=
